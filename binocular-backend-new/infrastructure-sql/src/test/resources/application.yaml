spring:
  profiles:
    active: test,postgres
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
    drop-first: true
  jpa:
    hibernate:
      ddl-auto: none
logging:
  file:
    path: logs
    name: logs/infrastructure-sql.test.log
  level:
    #    org.hibernate.orm.jdbc.bind: trace
    com.inso_world.binocular.infrastructure.sql.mapper: off
    com:
      inso_world:
        binocular: TRACE
      zaxxer:
        hikari: TRACE
